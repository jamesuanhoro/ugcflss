# ugcflss_describe_all <- function(
#     res_obj, stat = "median", interval = .89, convergence = FALSE, tau = .5) {
#   if (!(stat %in% c("median", "mean", "sd", "quantile"))) {
#     statement <- paste(
#       "`stat` must be one of \"median\", \"mean\", \"sd\" or \"quantile\",",
#       sep = " "
#     )
#     stop(statement)
#   }

#   # stat_post <- 
# }
